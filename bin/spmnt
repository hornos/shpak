#!/bin/bash

# header
# ------------------------------------------------
. $(dirname ${0})/../lib/h.sh
sp_f_load ssh


_f=""
if test "${1}" = "-f" ; then
  _f="${1}"
fi

# main
# ------------------------------------------------
sp_f_ptt "${sp_g_bn}"

for i in "mid" "key" ; do
  f="sp_p_${i}"
  mf="${!f}/${sp_p_ssh_key}.noenc"
  if test -r "${mf}" ; then
    efsmnt ${_f} -m "ssh${i}"
  else
    echo "mounted ${mf}"
  fi
done
