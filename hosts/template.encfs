# id
sp_g_efs_mid="encrypted"

# full path to the encrypted dir
sp_p_efs_edir="${sp_p_encfs}/${sp_g_efs_mid}.edir"

# full path of the unencrypted dir
sp_p_efs_udir="${sp_p_encfs}/${sp_g_efs_mid}.udir"

sp_g_efs_opts=""
